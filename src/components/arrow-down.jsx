import styled from 'styled-components';

import { Triangle } from 'react-feather';

const ArrowDown = styled(Triangle)`
  transform: rotate(180deg);
  stroke: #222222;
  fill: #444444;
`;

export default ArrowDown;
